{
  "name": "toolsapp",
  "version": "1.1.0",
  "private": true,
  "description": "Static site of self-contained tools, served by a small Express API for shared reference data (tax brackets, cost-of-living index) and the Exam tool's upload feature.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cookie": "^2.0.1",
    "express": "^4.21.2"
  }
}
