# Copy to .env (gitignored) and adjust for your environment.
# Loaded via Node's built-in --env-file flag (see package.json's start script).

PORT=8000

# Not used yet — placeholders for when auth lands on top of server/middleware/auth.js.
# API_KEYS=
# JWT_SECRET=
